# Repo-root ignores. The Next.js app lives in site/ with its own .gitignore.
.DS_Store
*.pem
*.log
.playwright-mcp/
# Vercel link now lives at the repo root (deploys run from here; root dir = "site").
.vercel
